"""
担当: AI・画像認識
画像から食材を検出させるためのプロンプトを設計する。
"""


def build_image_prompt() -> str:
    """
    Gemini（マルチモーダル）に渡す、食材検出用のプロンプトを返す。
    現在はスタブ。AI担当が実装する。

    実装例:
        return (
            "画像に写っている食材をすべて検出し、"
            "その名前・量・確信度(confidence)を抽出してください。"
        )

    精度を上げたい場合はここで以下のような工夫を追加する:
    - 食材名の表記ゆれを防ぐ指示（例:「一般的な食材名で」）
    - 量が読み取れない場合の扱い（例:「不明」とする）
    - confidenceの基準を具体的に指示する
    """
    raise NotImplementedError("担当: AI - build_image_prompt を実装してください")
